# Build artifacts
/bin/
/dist/
*.test
*.out
*.tmp

# Local databases
*.db
*.db-shm
*.db-wal

# Partial render outputs
*.partial

# Local secrets
secrets*.json
.env
.env.*
.smoketest/

# Rust build artifacts
crates/*/target/
target/

# Local-only tooling and evaluation data (never commit binaries or media)
/.tools/
/eval/
soak-results.json
.gotmp/

# Day-session scratch that once slipped into the tree: batch logs and
# one-off patch scripts stay local (see session #23)
/.badminton-*.log
/.patch*.py

# Night-ops records and automation config: kept locally, never published
/NIGHTOPS.yaml
/docs/NIGHTLY_PROGRESS.md
/docs/NIGHTLY_LOG.md
/docs/ACCEPTANCE.md
.badminton-ws/
